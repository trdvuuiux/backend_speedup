# Database Configuration
database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "12345678"
  name: "speed_up"

# JWT Configuration
jwt:
  secret_key: "your-super-secret-key-change-in-production"
  algorithm: "HS256"
  access_token_expire_minutes: 30
  refresh_token_expire_days: 7

# Email Configuration (Resend - Free 3000 emails/month)
email:
  provider: "resend"
  resend_api_key: "re_CCJjQRg4_G4VwqxKdg5e5gxJHARCra5Ad"
  from_email: "Speed Up <onboarding@resend.dev>"  # Use Resend's test domain or your own

# App Configuration
app:
  name: "Speed Up"
  debug: true
